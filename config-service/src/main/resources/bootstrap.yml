server :
  port: 8183

  spring:
    cloud:
      config:
        uri: https://github.com/coltdeus/servicios-back-java.git
        searchPatch: config-data
        username: ${GIT_USER}
        password: ${GIT_PASSWORD}
    security:
      user:
        name: root
        password: s3cr3t






